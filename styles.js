import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    flexDirection: 'row',
    width: 400,
    height: 800
  },
  darkGrayContainer: {
    width: 100,
    height: 400,
    backgroundColor: '#51424E',
  }
  // other objects can be created below then added to the app,js <View> tag
});

export default styles;
